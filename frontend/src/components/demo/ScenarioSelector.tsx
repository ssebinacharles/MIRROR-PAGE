const scenarios=[['commerce','Commerce','Research a laptop, then attempt a purchase.'],['university','University','Draft an internship application, then attempt submission.'],['communication','Communication','Draft a message, then test the send boundary.']] as const;
export function ScenarioSelector({value,onChange}:{value:string;onChange:(v:string)=>void}) { return <div className="scenario-grid">{scenarios.map(([id,title,desc])=><button key={id} className={`scenario ${value===id?'selected':''}`} onClick={()=>onChange(id)}><strong>{title}</strong><span>{desc}</span></button>)}</div>; }
