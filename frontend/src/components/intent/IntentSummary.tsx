import { Card } from '@/components/ui/Card'; import { Badge } from '@/components/ui/Badge'; import type { IntentContract } from '@/types/models';
export function IntentSummary({intent}:{intent:IntentContract|null}) { if (!intent) return <Card><div className="empty-state"><strong>No active intent</strong><p>Create an intent contract to give the agent bounded authority.</p></div></Card>; return <Card><div className="eyebrow">ACTIVE INTENT</div><div className="intent-goal">“{intent.goal}”</div><div className="tag-row"><Badge tone="success">{intent.status}</Badge>{intent.expires_at&&<Badge>expires {new Date(intent.expires_at).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})}</Badge>}</div><div className="intent-grid"><div><span>Allowed</span><strong>{intent.allowed_actions.length}</strong></div><div><span>Approval</span><strong>{intent.approval_required_actions.length}</strong></div><div><span>Denied</span><strong>{intent.denied_actions.length}</strong></div><div><span>Data scope</span><strong>{intent.data_scope.length}</strong></div></div></Card>; }
