import { Clock3 } from 'lucide-react'; import { Card } from '@/components/ui/Card'; import { Button } from '@/components/ui/Button'; import { Badge } from '@/components/ui/Badge'; import type { Approval } from '@/types/models'; import { api } from '@/services/api';
export function ApprovalRequest({approval,onDone}:{approval:Approval;onDone:()=>void}) { const decide=async(action:'approve'|'deny')=>{await api.decideApproval(approval.id,action); onDone();}; return <Card><div className="approval-head"><div><div className="eyebrow">ACTION REQUIRES APPROVAL</div><h3>{approval.tool_name}</h3></div><Badge tone="warning"><Clock3 size={13}/> pending</Badge></div><pre className="json-block">{JSON.stringify(approval.scope,null,2)}</pre><div className="action-row"><Button variant="ghost" onClick={()=>void decide('deny')}>Deny</Button><Button onClick={()=>void decide('approve')}>Approve once</Button></div></Card>; }
