import { Card } from '@/components/ui/Card'; import { RiskIndicator } from '@/components/security/RiskIndicator'; import type { Tool } from '@/types/models';
export function ToolCard({tool}:{tool:Tool}) { return <Card><div className="tool-head"><div><div className="tool-name">{tool.name}</div><div className="tool-title">{tool.title}</div></div><RiskIndicator risk={tool.risk_level}/></div><p>{tool.description}</p><div className="tool-facts"><span>{tool.read_only?'READ ONLY':'MUTATING'}</span><span>{tool.side_effect?'SIDE EFFECT':'NO SIDE EFFECT'}</span><span>{tool.reversible?'REVERSIBLE':'IRREVERSIBLE'}</span>{tool.financial&&<span>FINANCIAL</span>}</div></Card>; }
