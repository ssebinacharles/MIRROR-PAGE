import { useApp } from '@/app/providers'; import { SectionHeader } from '@/components/ui/SectionHeader'; import { ToolCard } from '@/components/tools/ToolCard'; import { EmptyState } from '@/components/ui/EmptyState';
export function ToolsPage(){const {tools}=useApp(); return <div><SectionHeader eyebrow="CAPABILITIES" title="Tool registry" description="WebMCP capabilities visible to the agent and their security posture."/><div className="card-grid">{tools.map(t=><ToolCard key={t.id} tool={t}/>)}</div>{!tools.length&&<EmptyState title="No backend tools loaded">Start the Django backend and refresh. Demo WebMCP tools remain available in-browser.</EmptyState>}</div>}
