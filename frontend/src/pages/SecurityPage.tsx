import { ShieldCheck, ShieldAlert, Database, KeyRound } from 'lucide-react'; import { SectionHeader } from '@/components/ui/SectionHeader'; import { Card } from '@/components/ui/Card'; import { Badge } from '@/components/ui/Badge'; import { useWebMCP } from '@/hooks/useWebMCP';
export function SecurityPage(){const {supported,registered}=useWebMCP(); return <div><SectionHeader eyebrow="SECURITY" title="Security posture" description="The controls MIRROR applies before an agent can create a side effect."/><div className="security-grid"><Card><div className="security-icon"><ShieldCheck size={18}/></div><h2>Fail-closed authorization</h2><p>Unknown or ambiguous actions are denied instead of silently permitted.</p><Badge tone="success">ENFORCED</Badge></Card><Card><div className="security-icon"><ShieldAlert size={18}/></div><h2>Intent drift</h2><p>Action context is compared against the active human intent and its constraints.</p><Badge tone="success">ACTIVE</Badge></Card><Card><div className="security-icon"><Database size={18}/></div><h2>Data minimization</h2><p>Agent access is bounded by the contract's declared data scope.</p><Badge tone="success">ACTIVE</Badge></Card><Card><div className="security-icon"><KeyRound size={18}/></div><h2>WebMCP boundary</h2><p>{supported&&registered?'WebMCP is detected and MIRROR tools are registered in this browser.':'WebMCP is not currently available in this browser runtime.'}</p><Badge tone={supported&&registered?'success':'warning'}>{supported&&registered?'READY':'STANDBY'}</Badge></Card></div></div>}
